function [data, updatedLabels] = WeightedTypeStatsLabels(types, typeLabels, stats)
    
    typeWeight = 0.4;
    statsWeight = 0.6;
    
    threshold = 0.6;

    for index = 1 : size(rollMinimaIndexes,1)
        score = types*typeWeight + 
    end

end